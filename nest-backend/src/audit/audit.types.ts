/** Domain labels stored on [AuditLog.entity] (string column). */
export enum AuditEntity {
  Account = 'Account',
  AccountTransfer = 'AccountTransfer',
  Budget = 'Budget',
  Expense = 'Expense',
}
